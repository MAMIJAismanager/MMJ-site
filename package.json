{
  "name": "mamija-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "npm run verify:public-release && nuxt dev",
    "build": "npm run verify:boundary && npm run verify:public-release && nuxt build --preset=static",
    "generate": "npm run verify:boundary && npm run verify:public-release && nuxt generate",
    "preview": "nuxt preview",
    "typecheck": "npm run verify:boundary && nuxt typecheck",
    "prepare": "nuxt prepare",
    "verify:boundary": "node scripts/public-boundary-gate.mjs",
    "verify:public-release": "node scripts/public-release-verify.mjs",
    "gate:mmj-05n-a": "npm run verify:boundary && npm run verify:public-release && npm run typecheck && npm run generate"
  },
  "dependencies": {
    "@pinia/nuxt": "0.11.2",
    "nuxt": "4.0.3",
    "pinia": "3.0.3"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vue-tsc": "3.0.6"
  }
}
